---
export interface Props {
    image_id: string;
}

const { image_id } = Astro.props;

const posts = await fetch(`https://colateralmkt.com/wp-json/wp/v2/media/${image_id}`);
const post = await posts.json();

---


<img src={post.guid.rendered} class="h-full w-full object-cover object-center" />